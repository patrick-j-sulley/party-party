import React from 'react'
import { connect } from 'react-redux'

const Drink = ({}) => {
  // img of drink
  // drink name
  // drinker name
  // ingreds

  return (
    <div>
      <p>hello from Drink!!</p>
    </div>
  )
}

// const mapStateToProps = () => ({})

export default connect(mapStateToProps)(Drink)